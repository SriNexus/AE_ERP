{
  "name": "neozy-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Neozy ERP — the single Cloud Function introduced by the Multi-Tenant Implementation Master Plan (§4.5): onUserDeactivated (revokeRefreshTokens on deactivation).",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^14.1.0",
    "firebase-functions": "^6.3.2"
  }
}
