/**
 * Monitoring feature types — re-exported from the workflow module
 *
 * This file exists so the feature folder has its own types barrel,
 * matching the architecture of every other module in the codebase.
 */

export type { GenerationReadingRecord } from '../../../lib/monitoringWorkflow';
