import type React from 'react';
export default function UniversalDisposableTab({ entityType }: { entityType?: string }): React.ReactNode { return <div className="p-6"><section className="rounded-xl border border-[var(--color-border-subtle)] bg-[var(--color-surface)] p-5"><h2 className="text-sm font-semibold text-[var(--color-text)]">Record disposal</h2><p className="mt-2 text-sm text-[var(--color-text-muted)]">{entityType === 'generation_reading' ? 'Generation readings are immutable historical records and cannot be deleted or disposed.' : 'No disposal action is available for this record.'}</p></section></div>; }