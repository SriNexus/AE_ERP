/**
 * useContextResolver — Convenience re-export
 *
 * Import from this module for a clean import path:
 *   import { useContextResolver } from '../../context/useContextResolver';
 *
 * The hook implementation lives in ContextResolver.tsx alongside
 * the provider to keep them tightly coupled.
 */

export { useContextResolver } from './ContextResolver';
